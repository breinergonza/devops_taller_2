# Configuracion del agente Python de New Relic.
# La licencia entra por NEW_RELIC_LICENSE_KEY.

[newrelic]

# Fallback; runtime puede pisarlo.
app_name = devops-taller-4-flask

# Envio datos a New Relic.
monitor_mode = true

# Logs del agente a stdout.
log_file = stdout
log_level = info

# SSL explicito.
ssl = true

# Flush corto al cerrar.
shutdown_timeout = 2.5

# Correlacion de trazas.
distributed_tracing.enabled = true
cross_application_tracer.enabled = false

# Trazas lentas y SQL obfuscado.
transaction_tracer.enabled = true
transaction_tracer.transaction_threshold = apdex_f
transaction_tracer.record_sql = obfuscated
transaction_tracer.stack_trace_threshold = 0.5
transaction_tracer.explain_enabled = true
transaction_tracer.explain_threshold = 0.5

# Captura de errores.
error_collector.enabled = true
error_collector.ignore_status_codes = 404

# API sin browser monitoring.
browser_monitoring.auto_instrument = false

# Perfilamiento bajo carga.
thread_profiler.enabled = true

# Logs de app hacia New Relic.
application_logging.enabled = true
application_logging.forwarding.enabled = true
application_logging.forwarding.max_samples_stored = 10000
application_logging.metrics.enabled = true
application_logging.local_decorating.enabled = true

# Perfiles por NEW_RELIC_ENVIRONMENT.

[newrelic:development]
# Dev: logs informativos.
monitor_mode = true
log_level = info
app_name = devops-taller-4-flask (development)

[newrelic:staging]
monitor_mode = true
log_level = info
app_name = devops-taller-4-flask (staging)

[newrelic:production]
# Prod: menos ruido.
monitor_mode = true
log_level = warning
app_name = devops-taller-4-flask (production)
